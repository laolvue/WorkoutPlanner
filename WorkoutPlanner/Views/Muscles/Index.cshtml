@model IEnumerable<WorkoutPlanner.Models.Muscle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.muscleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.muscleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.muscleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.muscleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.muscleId })
        </td>
    </tr>
}

</table>
