@model WorkoutPlanner.Models.Eventful

@{
    ViewBag.Title = "Create";
}
@{ 
    var prod5 = (List<SelectListItem>)ViewData["muscleName"];
}
@{
    var prod6 = (List<SelectListItem>)ViewData["dayName"];
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Eventful</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.Title, "Working On:", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-4">
               @*@Html.DropDownList("musclez", "select muscle")*@
                @Html.DropDownListFor(model => model.Title, prod5)
            </div>
            <div class="col-md-5">
                @Html.ActionLink("Add muscle", "Create", "Muscles")
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.Description, "Days:", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(model => model.Description, prod6)
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
