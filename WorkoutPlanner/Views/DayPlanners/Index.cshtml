@model IEnumerable<WorkoutPlanner.Models.DayPlanner>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Workout.workoutName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.startAt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.endAt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Workout.workoutName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.startAt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.endAt)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.dayPlannerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.dayPlannerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.dayPlannerId })
        </td>
    </tr>
}

</table>
